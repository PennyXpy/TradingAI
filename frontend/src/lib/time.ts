// // 将 UTC ISO 字符串 → 相对时间，如 “3 hours ago”
// import { formatDistanceToNow } from "date-fns";

// export function relativeTime(utcIso: string): string {
//   return formatDistanceToNow(new Date(utcIso), { addSuffix: true });
// }
